# OpenCode 适配层

这里放 OpenCode 侧接入 `product-dev-skill` 的说明。

第一次接手当前仓库，先看 `../START-HERE.md`。

## 当前约定

1. OpenCode 接入时仍以 `product-dev` 为统一业务入口
2. 方法层增强由 `product-dev-methods` 提供
3. OpenCode 端只维护适配说明，不自行派生第二套工作流

## 建议用途

- 会话初始化提示
- 原型文档化边界说明
- 平台特有命令或目录映射说明
