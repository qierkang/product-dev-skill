---
name: product-dev-skill
description: Use when 需要把原型 HTML、字段表或页面说明快速沉淀成结构化需求文档与 UI 交互文档，并希望按标准模板与 Gate 推进。
---

# Product Dev Skill

根入口只做路由，真实执行入口在：

- `skills/product-dev/SKILL.md`

方法增强在：

- `skills/product-dev-methods/SKILL.md`

UI 文档 companion 在：

- `skills/product-dev-ui/SKILL.md`

若当前运行端已安装 `ui-ux-pro-max`，UI 阶段优先协同它；若未安装，则自动回退到 `shared/references/design/`。

即使当前运行端还能识别 `frontend-design` 等通用设计 skill，也不得把它们当成 `ui-ux-pro-max` 缺失时的默认兜底。
