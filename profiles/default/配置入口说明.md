# 配置入口说明

第一次迁移到新机器，优先检查这三个入口：

1. `profile.yaml`
2. `.env`
3. `install/sync.sh`

## 需要改什么

- `requests_dir`：request 输出目录
- `examples_dir`：示例输出目录
- `default_owner`：默认作者
- `.env` 中各运行端目标 skill 目录

## 当前默认边界

- 只执行 `docs`
- 不连库
- 不启动服务
- 不进入测试、UAT、生产环境
